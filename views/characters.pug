extend layout

block content
    .container
        .row
            .col-4
                ul
                each character in characters
                    li
                        - var urlSlug = character.slug
                        a(href="/characters/" + urlSlug)= character.name 
                        //a(href="/characters/" + urlSlug): img(src=character.image)

    //- ul
    //- each character in characters
    //-     li
    //-         - var urlSlug = character.slug
    //-         a(href="/characters/" + urlSlug)= character.name